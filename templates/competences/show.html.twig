{% extends 'base.html.twig' %}

{% block title %}Competences{% endblock %}

{% block body %}
    <h1>Competences</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competence.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ competence.titre }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ competence.picture }}</td>
            </tr>
        </tbody>
    </table>
<div>
<div class="row justify-content-center justify-content-around">

    <a href="{{ path('competences_index') }}" class="btn btn-primary"><i class="fas fa-step-backward"></i></a>


   <a href="{{ path('competences_edit', {'id': competence.id}) }}" class="btn btn-warning"> <i class="far fa-edit"></i></a>


    {{ include('competences/_delete_form.html.twig') }}

</div>
</div>
{% endblock %}
